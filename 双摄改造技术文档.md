# streamer_codev6.0 双摄输入识别与推流改造技术文档

## 1. 文档目的

本文说明如何将 `streamer_codev5.0` 的单摄像头识别与 RTMP 推流程序改造成 `streamer_codev6.0` 的双摄像头版本，并记录涉及的模块、线程模型、数据流、格式兼容、性能优化和可靠性修复。

最终系统同时接入：

- MIPI 摄像头：`/dev/video11`，板端实测协商为 `NV12`、1280×720、多平面 V4L2。
- USB 摄像头：`/dev/video20`，板端实测协商为 `MJPG`、1280×720、单平面 V4L2。
- 两路画面分别经过 YOLOv5 RKNN 推理，绘制检测结果后合成为一帧 1280×720 图像。
- 合成图通过 RGA 转换为 NV12，经 Rockchip MPP 编码为 H.264，再由 FFmpeg 封装为 FLV 并推送到 RTMP 服务器。

## 2. 改造前后的总体架构

### 2.1 v5.0 单摄架构

```text
/dev/video0
    ↓ V4L2采集（固定YUYV）
读取队列
    ↓
RKNN线程池推理
    ↓
写入队列
    ↓
RGA BGR→NV12
    ↓
MPP H.264编码
    ↓
FFmpeg/RTMP推流
```

原程序存在以下单摄假设：

1. 摄像头设备固定为 `/dev/video0`。
2. 输入格式固定为 YUYV。
3. 只创建一个采集对象和一个采集线程。
4. 推理结果直接进入编码队列，不需要区分画面来源。
5. 输出尺寸等于单个摄像头尺寸。

### 2.2 v6.0 双摄架构

```text
/dev/video11（MIPI/NV12/mplane） ─→ 采集线程0 ─┐
                                               ├→ 非阻塞读取队列
/dev/video20（USB/MJPG/single） ─→ 采集线程1 ─┘
                                                      ↓
                                             3实例RKNN推理池
                                                      ↓
                                     按camera_id保存两路最新检测帧
                                                      ↓
                              左右双画面合成（最终1280×720）
                                                      ↓
                                             非阻塞编码队列
                                                      ↓
                                  RGA BGR→NV12 → MPP H.264
                                                      ↓
                                          FFmpeg FLV/RTMP
```

双摄版本不是将两路画面轮流写入一个推流通道，因为这样会导致接收端在两台摄像头之间闪烁；而是在推理完成后先合成一张包含两路画面的图，再进行一次编码和推流。

## 3. 主要修改文件

| 文件 | 主要修改 |
|---|---|
| `main.cpp` | 双 V4L2 设备、格式协商、多平面支持、双采集线程、来源标识、推理聚合、双画面合成、FPS 统计、模型路径解析 |
| `SafeQueue.h` | 增加非阻塞 `try_enqueue`、`try_dequeue`，队列满时丢帧而不是阻塞采集 |
| `thread_poll.h/.cpp` | 为每个 RKNN 模型实例增加互斥锁，消除同一 RKNN context 被并发访问的风险；删除逐帧调试日志 |
| `post_process.cpp` | 标签路径改为相对可执行文件解析；通过 `std::call_once` 线程安全加载标签；增加类别越界回退 |
| `yolov5s.cpp` | 完善模型文件打开、读取、内存分配和 RKNN 初始化失败处理，避免模型缺失时段错误 |
| `streamer.c` | MPP 输入、输出帧率改为灵活模式；删除逐帧输入日志 |
| `mpp.c` | H.264 参数适配 1280×720；删除逐帧编码日志和前五帧 H.264 测试落盘 |
| `rtmp.c` | 使用单调时钟生成 PTS/DTS；时间基改为毫秒；删除逐帧发送日志 |

## 4. V4L2 双摄像头适配

### 4.1 从固定格式改为格式枚举与优先级选择

v5.0 直接向设备设置 `V4L2_PIX_FMT_YUYV`，无法兼容当前两台格式不同的摄像头。v6.0 在 `V4L2Camera::open_device()` 中先调用 `VIDIOC_ENUM_FMT` 枚举设备支持的格式，再按照设备类型选择优先格式。

MIPI 格式优先级：

```text
NV12 → NV12M → NV21 → NV21M → YUYV → UYVY → I420/YV12 → MJPG
```

USB 格式优先级：

```text
MJPG → YUYV → UYVY → NV12/NV21
```

这样不会再向 `/dev/video11` 强制设置它不支持的 MJPG，同时 `/dev/video20` 可优先使用 MJPG，降低 USB 总线带宽。

### 4.2 同时兼容单平面和多平面设备

程序通过 `VIDIOC_QUERYCAP` 判断：

- `V4L2_CAP_VIDEO_CAPTURE_MPLANE`：使用 `V4L2_BUF_TYPE_VIDEO_CAPTURE_MPLANE`。
- `V4L2_CAP_VIDEO_CAPTURE`：使用 `V4L2_BUF_TYPE_VIDEO_CAPTURE`。

多平面模式为每个 V4L2 buffer 保存多个 `MappedPlane`，分别处理 `v4l2_plane` 的长度、偏移和 `bytesused`。单平面模式继续使用 `v4l2_buffer.m.offset`。

板端运行日志已经验证：

```text
[V4L2] opened /dev/video11 (NV12) 1280x720 @30fps mplane
[V4L2] opened /dev/video20 (MJPG) 1280x720 @30fps single-plane
```

### 4.3 输入格式统一转换为 BGR

两路输入最终都转换成 OpenCV BGR 图像，供 RKNN 预处理和画面合成使用：

- MJPG：`cv::imdecode()`。
- YUYV/UYVY：`cv::cvtColor()`。
- NV12/NV21：单平面直接包装，多平面先复制 Y、UV plane 到连续缓冲区，再 `cv::cvtColor()`。
- RGB24/BGR24、I420/YV12：使用对应的 OpenCV 转换方式。

## 5. 双路采集、队列和帧来源标识

### 5.1 FrameData 扩展

单摄版本只需要 `frame` 和全局 `index`。双摄版本增加：

```cpp
struct FrameData {
    cv::Mat frame;
    uint64_t index;
    uint64_t camera_sequence;
    int camera_id;
};
```

- `camera_id=0`：MIPI。
- `camera_id=1`：USB。
- `camera_sequence`：该摄像头自己的帧序号，用于防止较旧的异步推理结果覆盖较新的结果。
- `index`：全局任务编号，用于分配 RKNN 模型实例。

### 5.2 两个独立采集线程

主函数创建两个 `V4L2Camera` 和两个 `read_thread`：

```text
MIPI reader → camera_id 0
USB reader  → camera_id 1
```

两个线程共享 `g_read_queue`，但每帧带有来源信息，因此后续可以把推理结果放回正确的画面槽位。

### 5.3 非阻塞实时队列

原队列在满时等待，双摄输入达到约 60 帧/秒时容易造成采集线程阻塞和延迟持续累积。`SafeQueue` 新增：

```cpp
bool try_enqueue(const T& value);
bool try_enqueue(T&& value);
bool try_dequeue(T& value);
```

当推理或编码跟不上时，系统丢弃当前帧而不阻塞摄像头。实时视频优先保证“最新画面”，而不是保证每一帧都被处理。

## 6. RKNN 双摄并发推理

程序保留 3 个 `Yolov5s`/RKNN 实例，对应 RK3588 的三个 NPU core。双摄帧共同提交到 `ThreadPoll`。

原线程池任务根据任务编号选择模型，但任务可能由任意 worker 执行，存在两个 worker 同时调用同一个 RKNN context 的风险。v6.0 为每个模型实例增加独立互斥锁：

```cpp
std::vector<std::shared_ptr<std::mutex>> yolo_mutexes;
```

推理前锁定对应模型，确保一个 RKNN context 同一时刻只执行一个任务，不同模型实例仍可并行运行。

推理任务完成后返回：

```cpp
struct ProcessResult {
    cv::Mat processed_img;
    detect_result_group_t detection_results;
    bool success;
    std::string error_msg;
};
```

其中 `processed_img` 已绘制检测框和标签。

## 7. 双画面合成与 1280×720 输出

`inference_and_compositor_thread()` 保存两路最新推理图像：

```text
latest[0] = 最新MIPI检测画面
latest[1] = 最新USB检测画面
```

两路都至少产生一帧后，调用 `compose_side_by_side()` 生成最终画面。

最终输出固定为 1280×720，左右各分配一个 640×720 区域：

```text
┌──────────────────────1280──────────────────────┐
│       MIPI：640×720      │      USB：640×720   │
│   原画等比缩为640×360    │   原画等比缩为640×360│
│       垂直居中显示       │       垂直居中显示    │
└────────────────────────────────────────────────┘
```

`fit_into_tile()` 按比例缩放，保证画面不变形、不裁剪。每个区域左上角叠加 `MIPI /dev/video11` 或 `USB /dev/video20` 标识。

合成触发条件为：

1. 两路均有新推理结果；或
2. 距离上次输出超过 200 ms，使用另一路的最近结果补齐。

因此某一路短暂变慢时不会无限期阻塞整条推流链路。

## 8. BGR、NV12、MPP 和 FFmpeg 推流

### 8.1 RGA 转换

合成后的 BGR 图像通过 RGA 转换为 MPP 所需的 NV12：

```text
OpenCV BGR → RK_FORMAT_BGR_888 → RK_FORMAT_YCbCr_420_SP
```

NV12 缓冲区按照 MPP 的 16/64 字节对齐要求分配。

### 8.2 MPP 编码

最终编码参数：

- 分辨率：1280×720。
- 编码：H.264 High Profile。
- Level：3.1。
- 像素格式：NV12/YUV420SP。
- GOP：参考帧率的 2 倍。
- 码率：根据 1280×720 和参考帧率计算，30 FPS 时约 3.3 Mbps。

`fps_in_flex` 和 `fps_out_flex` 设置为 1，允许双摄推理和合成导致的帧间隔变化，不要求 MPP 强制补成固定帧率。

### 8.3 FFmpeg 时间戳

原 `rtmp.c` 将帧间隔写死为 `1000/30`，摄像头或推理速度变化时会产生错误时间戳。v6.0 使用：

```cpp
av_gettime_relative()
```

基于单调时钟生成毫秒级 PTS/DTS，并将 FFmpeg stream/codec time base 设置为 `1/1000`。配置帧率只作为码控、GOP 和播放器提示的参考值，实际包时间戳反映真实输出间隔。

## 9. 模型和标签路径可靠性修复

### 9.1 RKNN 模型路径

原模型路径 `../model/yolov5s.rknn` 依赖程序启动目录。从项目根目录运行 `./build_v6/app` 时会错误解析为 `~/model/yolov5s.rknn`，导致模型加载失败和段错误。

v6.0 通过 `/proc/self/exe` 获取可执行文件路径，再定位：

```text
可执行文件目录/../model/yolov5s.rknn
```

同时在模型不存在、读取不完整、内存分配失败或 `rknn_init` 失败时抛出明确错误，不再继续访问无效指针。

### 9.2 标签路径和并发加载

标签文件也改为相对可执行文件定位：

```text
可执行文件目录/../model/coco_80_labels_list.txt
```

三个 NPU worker 可能同时首次进入后处理。原来的 `static bool` 不能保护全局 `labels` vector，并发写入会造成随机崩溃。v6.0 使用：

```cpp
std::once_flag labels_once;
std::call_once(labels_once, load_labels_once);
```

确保标签只由一个线程加载一次。若文件缺失或数量不是 80，则生成 `class_0`～`class_79` 回退标签；类别编号也进行范围检查，并使用 `snprintf` 保证字符串结尾安全。

## 10. 推流流畅度优化

双摄初版在每帧执行多次同步日志输出，包括：

- 任务提交和 worker 获取任务日志。
- 每帧 NV12 输入日志。
- 每个 MPP 编码包的详细字符串拼接和打印。
- 每个 RTMP 包的 PTS/DTS 打印。
- 前五帧 H.264 文件落盘。

这些操作在双摄约 60 个输入任务/秒时会显著争抢 CPU、终端锁和磁盘 IO。最终版本删除了所有逐帧成功日志和测试落盘，只保留初始化信息与错误信息。

程序每两秒输出一次汇总统计：

```text
[FPS] capture=30.0/30.0 infer=29.0/30.0 push=25.5 drops=0/0 composite=0 errors=0
```

字段含义：

| 字段 | 含义 |
|---|---|
| `capture=A/B` | MIPI/USB 实际采集 FPS |
| `infer=A/B` | MIPI/USB 完成推理的 FPS |
| `push=N` | 成功进入 MPP/RTMP 的合成帧 FPS |
| `drops=A/B` | 两路因推理队列满而丢弃的帧数 |
| `composite=N` | 因编码队列满而丢弃的合成帧数 |
| `errors=N` | MPP/RTMP 处理失败次数 |

实测优化后采集约 30/30 FPS、推理约 26～32 FPS、推流约 25～31 FPS。

## 11. 构建和运行

开发板一行构建运行命令：

```bash
cd ~/streamer_codev6.0 && cmake -S . -B build_v6 && cmake --build build_v6 -j4 && ./build_v6/app /dev/video11 /dev/video20 rtmp://192.168.1.30:1935/live/app 1280 720 30
```

参数顺序：

```text
app MIPI设备 USB设备 RTMP地址 采集宽度 采集高度 帧率回退值 [可选RKNN模型路径]
```

最后的 `30` 只在驱动没有报告实际帧率时作为回退值；程序不会调用 `VIDIOC_S_PARM` 强制摄像头帧率。

虚拟机端一行播放命令：

```bash
sudo systemctl start nginx && ffplay -fflags nobuffer -flags low_delay -framedrop -sync ext rtmp://127.0.0.1:1935/live/app
```

## 12. 验证结果和判定标准

正常启动应出现：

```text
[V4L2] opened /dev/video11 (NV12) 1280x720 @30fps mplane
[V4L2] opened /dev/video20 (MJPG) 1280x720 @30fps single-plane
[Labels] loaded 80 labels from .../model/coco_80_labels_list.txt
[Main] dual-camera detection started: /dev/video11 + /dev/video20 -> 1280x720
```

接收端应同时看到左侧 MIPI、右侧 USB，且两路都绘制检测框。连续运行期间应满足：

- `errors=0`。
- `composite` 通常为 0。
- `drops` 不持续快速增长。
- `push` 接近较慢一路的 `infer` FPS。
- 不再出现 `open model failed`、`labels.size()=0` 或段错误。

## 13. 当前设计边界

1. 当前是“双画面展示与独立识别”，不是双目视觉；没有进行内外参标定、时间同步、深度估计或跨摄像头目标关联。
2. 两路 30 FPS 输入意味着每秒最多约 60 次推理。若替换为更大的 RKNN 模型，NPU 吞吐不足时会主动丢帧以维持低延迟。
3. MJPG 解码目前使用 OpenCV 软件解码。若 CPU 占用过高，可进一步改为 MPP/JPEG 硬件解码。
4. 当前输出布局为完整画面等比缩放，640×360 图像位于各自 640×720 区域中央。若希望占满整个 1280×720，可改为裁剪或画中画布局，但会牺牲部分视野或改变比例。

## 14. 总结

本次改造的核心不是简单再打开一个摄像头，而是把原来的单源串行假设改造成可区分来源的双源实时流水线：V4L2 自动协商不同设备格式，双线程采集，RKNN 多实例并发推理，按来源聚合结果，合成单一稳定画面，再通过 RGA、MPP 和 FFmpeg 推流。同时补齐了非阻塞队列、可变时间戳、模型/标签路径、线程安全和性能日志等工程化问题。
